> ⚠ This package is primarily intended for personal use.
> Only works in Vite projects. All `debug.*` output is disabled in production builds.

# vt-debug-console-vite
Vite プロジェクト専用の開発用デバッグコンソールラッパーです。
利用側の `import.meta.env.MODE` に応じてログ出力が切り替わります。
